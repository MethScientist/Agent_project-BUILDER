using Game.Core;
using UnityGame.Scripts;

namespace Game
{
    public class Player
    {
        private Health health;

        public Player()
        {
            health = new Health();
        }

        public void Hit(int dmg)
        {
            health.TakeDamage(dmg);
        }
    }
}